---
import Layout from '../layouts/Layout.astro';
const showcase = [
  { title: 'Core System', note: 'Your hosted AI backbone that reads, decides, and reports with control and safeguards.' },
  { title: 'Lead Generation Engine', note: 'Signal-driven outreach that learns your markets and produces qualified conversations.' },
  { title: 'Cashflow Copilot', note: 'Daily clarity on revenue, risk, and cash so owners act early with confidence.' },
];
const showcaseChips = {
  'Core System': ['Reads', 'Decides', 'Reports'],
  'Lead Generation Engine': ['Finds', 'Scores', 'Preps'],
  'Cashflow Copilot': ['Extracts', 'Forecasts', 'Flags'],
};
const steps = [
  { title: 'Diagnose', text: 'We study how the business really runs, where time is lost, and what clarity is missing.' },
  { title: 'Design', text: 'We outline a system that mirrors your operations with clear ownership and guardrails.' },
  { title: 'Build', text: 'We assemble the hosted AI layer so it interprets inputs and returns decisive actions.' },
  { title: 'Deploy & Iterate', text: 'We run the system with you, refine it, and keep it steady as conditions change.' },
];
---

<Layout>
	<main class="flex flex-col gap-16">
		<section class="relative overflow-hidden px-6 py-12 md:px-10 lg:px-12">
			<div class="absolute inset-0 opacity-[0.2] blur-3xl glow-animate bg-[radial-gradient(circle_at_18%_30%,rgba(10,170,231,0.2),transparent_45%),radial-gradient(circle_at_78%_55%,rgba(171,113,247,0.18),transparent_42%)]"></div>
			<div class="relative mx-auto max-w-5xl">
				<div class="ka-card rounded-[22px] border border-[var(--border)]/70 p-8 shadow-[0_18px_60px_-45px_rgba(0,0,0,0.35)] backdrop-blur">
					<div class="space-y-6 text-[var(--text)]">
						<p class="inline-flex items-center rounded-full border border-[var(--border)]/80 bg-white/70 px-3 py-1 text-xs font-medium text-[var(--muted)]">
							Bespoke, hosted AI systems
						</p>
						<h1 class="text-4xl font-semibold leading-tight tracking-tight text-[var(--text)] md:text-5xl">
							<span class="text-[var(--primary)]">Bespoke, hosted AI systems</span> — built around how your business actually works.
						</h1>
						<p class="text-lg text-[var(--muted)] md:text-xl">
							We design, build, and run end-to-end AI that removes manual work, interprets business data, and gives owners confident control over how the business runs.
						</p>
						<div class="flex flex-wrap gap-4">
							<a class="js-open-calendly rounded-full bg-[var(--accent-teal)] px-5 py-3 text-sm font-medium text-white shadow-sm transition duration-200 hover:-translate-y-0.5 hover:bg-[var(--secondary)]" href="/contact">
								Book a call
							</a>
							<a class="rounded-full border border-[var(--border)] bg-white/90 px-5 py-3 text-sm font-medium text-[var(--text)] shadow-sm transition duration-200 hover:-translate-y-0.5 hover:border-[var(--border)]/80 hover:shadow-md" href="/showcase">
								Watch the short demos
							</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section class="relative space-y-6 overflow-hidden rounded-3xl bg-white/80 p-6 shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<div class="pointer-events-none absolute inset-0 opacity-[0.06]">
				<div class="absolute inset-0 bg-[radial-gradient(circle_at_25%_20%,rgba(10,170,231,0.25),transparent_35%),radial-gradient(circle_at_80%_40%,rgba(171,113,247,0.22),transparent_35%)]"></div>
				<div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(10,170,231,0.08)_1px,transparent_1px),linear-gradient(0deg,rgba(171,113,247,0.08)_1px,transparent_1px)] bg-[size:120px_120px]"></div>
			</div>
			<div class="flex items-center justify-between">
				<h2 class="text-2xl font-semibold text-[var(--text)]">Showcase</h2>
				<a class="text-sm font-medium text-[var(--primary)] hover:text-[var(--secondary)]" href="/showcase">See details</a>
			</div>
			<div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
				{showcase.map((item) => (
					<div class="gradient-frame relative rounded-3xl p-[1.5px] transition duration-200 hover:-translate-y-1 hover:shadow-[0_20px_50px_-25px_rgba(0,0,0,0.45)]">
						<div class="ka-card flex h-full flex-col justify-between gap-5 rounded-[22px] p-5 shadow-md backdrop-blur">
							<div class="space-y-3">
								<p class="text-lg font-semibold text-[var(--text)]">{item.title}</p>
								<p class="text-sm text-[var(--muted)]/90">{item.note}</p>
								<div class="flex flex-wrap gap-2">
									{showcaseChips[item.title]?.map((chip) => (
										<span class="rounded-full bg-[var(--primary)]/10 px-2 py-1 text-[11px] font-medium text-[var(--text)]/80">
											{chip}
										</span>
									))}
								</div>
							</div>
							<a class="inline-flex w-fit items-center justify-center rounded-full border border-[var(--border)] bg-white px-4 py-2 text-sm font-medium text-[var(--text)] transition hover:-translate-y-0.5 hover:shadow-lg" href="/showcase">
								Watch demo
							</a>
						</div>
					</div>
				))}
			</div>
		</section>

		<section class="text-center space-y-3 px-4 md:px-10">
			<p class="text-lg font-medium text-[var(--text)]">Systems you can trust, built around how your business truly runs.</p>
			<p class="text-[var(--muted)]">Clear inputs, deliberate decisions, and steady ownership—no noise added.</p>
		</section>

		<section class="relative space-y-6 rounded-3xl bg-white/65 p-6 shadow-[0_10px_50px_-45px_rgba(0,0,0,0.45)] backdrop-blur md:p-8">
			<div class="pointer-events-none absolute inset-0 opacity-[0.05]">
				<div class="absolute inset-0 bg-[radial-gradient(circle_at_15%_30%,rgba(10,170,231,0.22),transparent_38%),radial-gradient(circle_at_85%_60%,rgba(171,113,247,0.2),transparent_36%)]"></div>
				<div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(10,170,231,0.08)_1px,transparent_1px),linear-gradient(0deg,rgba(171,113,247,0.08)_1px,transparent_1px)] bg-[size:140px_140px]"></div>
				<div class="absolute inset-0 bg-gradient-to-br from-white/30 via-transparent to-white/10"></div>
			</div>
			<h2 class="text-2xl font-semibold text-[var(--text)]">Process</h2>
			<div class="relative">
				<div class="absolute inset-x-4 top-1/2 hidden h-[2px] bg-gradient-to-r from-[var(--primary)]/30 via-[var(--accent)]/18 to-[var(--accent2)]/24 lg:block"></div>
				<div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
					{steps.map((step, index) => (
						<div class={`gradient-frame relative rounded-2xl p-[1.5px] transition duration-200 ${
							index === 0 ? 'lg:-translate-y-1' : index === 1 ? 'lg:-translate-y-2' : index === 2 ? 'lg:translate-y-2' : 'lg:translate-y-1'
						}`}>
							<div class="absolute -left-2 top-10 hidden h-3 w-3 -translate-y-1/2 rounded-full bg-[var(--primary)]/18 ring-2 ring-[var(--primary)]/20 lg:block"></div>
							<div class="ka-card flex h-full flex-col gap-3 rounded-[18px] border border-[var(--border)]/70 p-5 shadow-sm backdrop-blur">
								<div class="flex items-center gap-2 text-sm font-semibold text-[var(--muted)]">
									<span class="flex h-8 w-8 items-center justify-center rounded-full bg-[var(--primary)]/12 text-[var(--primary)] shadow-[0_0_0_8px_rgba(10,170,231,0.08)]">
										{String(index + 1).padStart(2, '0')}
									</span>
									<span>{step.title}</span>
								</div>
								<p class="text-[var(--muted)]">{step.text}</p>
							</div>
						</div>
					))}
				</div>
			</div>
		</section>
	</main>
</Layout>
