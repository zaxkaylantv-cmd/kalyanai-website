---
import Layout from '../layouts/Layout.astro';
const showcase = [
  { title: 'Core System', note: 'Your hosted AI backbone that reads, decides, and reports with control and safeguards.' },
  { title: 'Lead Generation Engine', note: 'Signal-driven outreach that learns your markets and produces qualified conversations.' },
  { title: 'Cashflow Copilot', note: 'Daily clarity on revenue, risk, and cash so owners act early with confidence.' },
];
const showcaseChips = {
  'Core System': ['Reads', 'Decides', 'Reports'],
  'Lead Generation Engine': ['Finds', 'Scores', 'Preps'],
  'Cashflow Copilot': ['Extracts', 'Forecasts', 'Flags'],
};
const steps = [
  { title: 'Diagnose', text: 'We study how the business really runs, where time is lost, and what clarity is missing.' },
  { title: 'Design', text: 'We outline a system that mirrors your operations with clear ownership and guardrails.' },
  { title: 'Build', text: 'We assemble the hosted AI layer so it interprets inputs and returns decisive actions.' },
  { title: 'Deploy & Iterate', text: 'We run the system with you, refine it, and keep it steady as conditions change.' },
];
---

<Layout
	title="Kalyan AI — Bespoke, hosted AI systems"
	description="We design, build, and host bespoke AI systems that remove friction from core business workflows."
>
	<main class="flex flex-col gap-16">
		<section class="relative overflow-hidden px-6 py-12 md:px-10 lg:px-12">
			<div class="absolute inset-0 opacity-[0.2] blur-3xl glow-animate bg-[radial-gradient(circle_at_18%_30%,rgba(10,170,231,0.2),transparent_45%),radial-gradient(circle_at_78%_55%,rgba(171,113,247,0.18),transparent_42%)]"></div>
			<div class="relative mx-auto max-w-5xl">
				<div class="ka-card rounded-[22px] p-8 shadow-[0_18px_60px_-45px_rgba(0,0,0,0.35)] backdrop-blur">
					<div class="space-y-6 text-[var(--text)]">
						<p class="inline-flex items-center rounded-full border border-[var(--border-subtle)] bg-white px-3 py-1 text-xs font-medium text-[var(--text)]">
							Bespoke, hosted AI systems
						</p>
						<h1 class="text-4xl font-semibold leading-tight tracking-tight text-[var(--brand-navy)] md:text-5xl">
							<span>Bespoke, hosted AI systems</span> <span class="text-[var(--primary)]">— built around how your business actually works.</span>
						</h1>
						<p class="copy-narrow text-lg text-[var(--muted)] md:text-xl">
							We design, build, and host bespoke AI systems that remove manual work, interpret business data, and give owners confident control over how the business runs.
						</p>
						<div class="flex flex-wrap gap-4">
							<a class="js-open-calendly rounded-full bg-[var(--accent-teal)] px-5 py-3 text-sm font-medium text-white shadow-sm transition duration-200 hover:-translate-y-0.5 hover:bg-[var(--secondary)]" href="/contact">
								Book a call
							</a>
							<a class="rounded-full border border-[var(--border-subtle)] bg-white px-5 py-3 text-sm font-medium text-[var(--text)] shadow-sm transition duration-200 hover:-translate-y-0.5 hover:shadow-md" href="/how-the-systems-work">
								Watch the walkthroughs
							</a>
						</div>
					</div>
				</div>
			</div>
		</section>

		<section class="relative space-y-6 overflow-hidden rounded-3xl bg-white/80 p-6 text-center shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<div class="pointer-events-none absolute inset-0 opacity-[0.06]">
				<div class="absolute inset-0 bg-[radial-gradient(circle_at_25%_20%,rgba(10,170,231,0.25),transparent_35%),radial-gradient(circle_at_80%_40%,rgba(171,113,247,0.22),transparent_35%)]"></div>
				<div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(10,170,231,0.08)_1px,transparent_1px),linear-gradient(0deg,rgba(171,113,247,0.08)_1px,transparent_1px)] bg-[size:120px_120px]"></div>
			</div>
			<div class="flex flex-col items-center gap-1">
				<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">Showcase</h2>
				<p class="text-base text-[var(--primary)]">The walkthroughs are examples of systems we’ve built. Every client system is bespoke.</p>
				<a class="text-sm font-medium text-[var(--brand-navy)] hover:text-[var(--brand-navy-light)]" href="/showcase">See details</a>
			</div>
			<div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
				{showcase.map((item) => (
					<div class="gradient-frame relative rounded-3xl p-px transition duration-200 hover:-translate-y-1 hover:shadow-[0_20px_50px_-25px_rgba(0,0,0,0.45)]">
						<div class="ka-card flex h-full flex-col justify-between gap-5 rounded-[22px] p-5 text-center shadow-md backdrop-blur">
							<div class="space-y-3">
								<p class="text-lg font-semibold text-[var(--brand-navy)]">{item.title}</p>
								<p class="text-sm text-[var(--muted)]/90">{item.note}</p>
								<div class="flex flex-wrap justify-center gap-2">
									{showcaseChips[item.title]?.map((chip) => (
										<span class="rounded-full bg-[var(--primary)]/10 px-2 py-1 text-[11px] font-medium text-[var(--text)]/80">
											{chip}
										</span>
									))}
								</div>
							</div>
							<a class="inline-flex w-fit items-center justify-center rounded-full border border-transparent bg-[var(--primary)] px-4 py-2 text-sm font-medium text-white transition hover:-translate-y-0.5 hover:bg-[var(--secondary)] mx-auto" href="/showcase">
								Watch the walkthroughs
							</a>
						</div>
					</div>
				))}
			</div>
		</section>

		<section class="ka-card text-center space-y-3 rounded-3xl bg-white/80 px-4 py-8 md:px-10">
			<p class="copy-narrow text-3xl font-semibold text-[var(--text)]">Systems you can trust, built around how your business truly runs.</p>
			<p class="copy-narrow text-lg text-[var(--primary)] text-center mx-auto max-w-2xl">Clear inputs, deliberate decisions, and steady ownership—no noise added.</p>
		</section>

		<section class="ka-card space-y-6 rounded-3xl bg-white/80 p-6 text-center shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">Examples of systems we build</h2>
			<p class="copy-narrow text-lg text-[var(--primary)] text-center block mx-auto max-w-3xl -mt-1">Every build is bespoke. These are common outcomes we deliver for SMEs.</p>
			<div class="grid justify-items-center gap-4 md:grid-cols-2">
				<div class="w-full max-w-xl space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Sales follow-up and proposals</p>
					<p class="text-[var(--muted)]">Keeps opportunities current, drafts proposals with guardrails, and prompts owners to close loops faster.</p>
				</div>
				<div class="w-full max-w-xl space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Document intake and financial visibility</p>
					<p class="text-[var(--muted)]">Extracts key figures and dates, rolls them into daily views, and flags cash or obligation risks early.</p>
				</div>
				<div class="w-full max-w-xl space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Lead qualification and outreach preparation</p>
					<p class="text-[var(--muted)]">Prepares short, contextual summaries so outreach focuses on qualified leads with the right next action.</p>
				</div>
				<div class="w-full max-w-xl space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Operational reporting and action queues</p>
					<p class="text-[var(--muted)]">Turns scattered updates into concise reports with accountable action lists for operators and managers.</p>
				</div>
				<div class="w-full max-w-xl space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur md:col-span-2">
					<p class="font-semibold text-[var(--brand-navy)]">Client onboarding and service delivery</p>
					<p class="text-[var(--muted)]">Guides teams through consistent onboarding, tracks service steps, and keeps clients informed without extra chasing.</p>
				</div>
			</div>
			<div class="space-y-2 text-center">
				<p class="text-sm text-[var(--muted)]">
					The walkthroughs are examples of systems we’ve built — your system is designed around your operations.
				</p>
				<a class="mx-auto inline-flex items-center justify-center rounded-full border border-transparent bg-[var(--primary)] px-4 py-2 text-sm font-medium text-white transition hover:-translate-y-0.5 hover:bg-[var(--secondary)]" href="/how-the-systems-work">
					Watch the walkthroughs
				</a>
			</div>
		</section>

		<section class="ka-card space-y-6 rounded-3xl bg-white/80 p-6 text-center shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">How we work</h2>
			<div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
				<div class="space-y-2 text-center">
					<p class="text-sm font-semibold text-[var(--brand-navy)]">Discover</p>
					<p class="copy-narrow text-[var(--muted)]">We learn how the business really runs and where friction slows work.</p>
				</div>
				<div class="space-y-2 text-center">
					<p class="text-sm font-semibold text-[var(--brand-navy)]">Design</p>
					<p class="copy-narrow text-[var(--muted)]">We outline a hosted AI system that mirrors your workflows with clear owners and guardrails.</p>
				</div>
				<div class="space-y-2 text-center">
					<p class="text-sm font-semibold text-[var(--brand-navy)]">Build</p>
					<p class="copy-narrow text-[var(--muted)]">We assemble and test the hosted layer so decisions and handoffs happen reliably.</p>
				</div>
				<div class="space-y-2 text-center">
					<p class="text-sm font-semibold text-[var(--brand-navy)]">Host &amp; Improve</p>
					<p class="copy-narrow text-[var(--muted)]">We operate, monitor, and refine so performance and oversight stay aligned.</p>
				</div>
			</div>
		</section>

		<section class="space-y-6 rounded-3xl bg-white/80 p-6 text-center shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">Built for trust</h2>
			<div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
				<div class="space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Clear ownership and approvals</p>
					<p class="text-[var(--muted)]">Every decision has an owner, approvals are explicit, and nothing moves without a named accountable person.</p>
				</div>
				<div class="space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Guardrails and auditability</p>
					<p class="text-[var(--muted)]">Controls, logs, and checkpoints are built in so you can see what happened and why.</p>
				</div>
				<div class="space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Evidence and reporting</p>
					<p class="text-[var(--muted)]">Signals and actions are summarised into concise reports that leaders can review quickly.</p>
				</div>
				<div class="space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Monitoring and support</p>
					<p class="text-[var(--muted)]">We host and monitor the system, handle issues, and keep it aligned with your operating reality.</p>
				</div>
				<div class="space-y-1 rounded-2xl bg-white/70 p-4 text-center shadow-sm backdrop-blur">
					<p class="font-semibold text-[var(--brand-navy)]">Training and handover</p>
					<p class="text-[var(--muted)]">We train your team and leave clear documentation so the system sticks.</p>
				</div>
			</div>
		</section>

		<section class="ka-card relative space-y-6 rounded-3xl bg-white/65 p-6 text-center shadow-[0_10px_50px_-45px_rgba(0,0,0,0.45)] backdrop-blur md:p-8">
			<div class="pointer-events-none absolute inset-0 opacity-[0.05]">
				<div class="absolute inset-0 bg-[radial-gradient(circle_at_15%_30%,rgba(10,170,231,0.22),transparent_38%),radial-gradient(circle_at_85%_60%,rgba(171,113,247,0.2),transparent_36%)]"></div>
				<div class="absolute inset-0 bg-[linear-gradient(90deg,rgba(10,170,231,0.08)_1px,transparent_1px),linear-gradient(0deg,rgba(171,113,247,0.08)_1px,transparent_1px)] bg-[size:140px_140px]"></div>
				<div class="absolute inset-0 bg-gradient-to-br from-white/30 via-transparent to-white/10"></div>
			</div>
			<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">Process</h2>
			<div class="relative">
				<div class="absolute inset-x-4 top-1/2 hidden h-[2px] bg-gradient-to-r from-[var(--primary)]/30 via-[var(--accent)]/18 to-[var(--accent2)]/24 lg:block"></div>
				<div class="grid gap-6 md:grid-cols-2 lg:grid-cols-4">
					{steps.map((step, index) => (
						<div class={`relative rounded-2xl transition duration-200 ${
							index === 0 ? 'lg:-translate-y-1' : index === 1 ? 'lg:-translate-y-2' : index === 2 ? 'lg:translate-y-2' : 'lg:translate-y-1'
						}`}>
							<div class="absolute -left-2 top-10 hidden h-3 w-3 -translate-y-1/2 rounded-full bg-[var(--primary)]/18 ring-2 ring-[var(--primary)]/20 lg:block"></div>
						<div class="ka-card flex h-full flex-col gap-3 rounded-[18px] border border-[var(--border-subtle)] p-5 text-center shadow-sm backdrop-blur">
								<div class="flex items-center justify-center gap-2 text-sm font-semibold text-[var(--brand-navy)]">
									<span class="flex h-8 w-8 items-center justify-center rounded-full bg-[var(--primary)]/12 text-[var(--brand-navy)] shadow-[0_0_0_8px_rgba(10,170,231,0.08)]">
										{String(index + 1).padStart(2, '0')}
									</span>
									<span>{step.title}</span>
								</div>
								<p class="text-[var(--muted)]">{step.text}</p>
							</div>
						</div>
					))}
				</div>
			</div>
		</section>

		<section class="ka-card space-y-4 rounded-3xl bg-white/80 p-6 text-center shadow-[0_10px_50px_-40px_rgba(0,0,0,0.5)] backdrop-blur md:p-8">
			<h2 class="text-2xl font-semibold text-[var(--brand-navy)]">Outcomes you can measure</h2>
			<ul class="mx-auto space-y-2 text-center text-[var(--muted)]">
				<li class="flex items-start justify-center gap-2">
					<span class="mt-1 h-1.5 w-1.5 rounded-full bg-[var(--primary)]/60"></span>
					<span>Hours back each week through fewer manual handoffs and checks.</span>
				</li>
				<li class="flex items-start justify-center gap-2">
					<span class="mt-1 h-1.5 w-1.5 rounded-full bg-[var(--primary)]/60"></span>
					<span>Fewer errors because context stays consistent and reviews are baked in.</span>
				</li>
				<li class="flex items-start justify-center gap-2">
					<span class="mt-1 h-1.5 w-1.5 rounded-full bg-[var(--primary)]/60"></span>
					<span>Faster follow-up on leads and cases with clear ownership.</span>
				</li>
				<li class="flex items-start justify-center gap-2">
					<span class="mt-1 h-1.5 w-1.5 rounded-full bg-[var(--primary)]/60"></span>
					<span>Clearer visibility on progress, risks, and actions for leaders.</span>
				</li>
			</ul>
			<div class="pt-2 flex justify-center">
				<a
					class="inline-flex items-center justify-center rounded-full bg-[var(--primary)] px-5 py-3 text-sm font-medium text-white shadow-sm transition hover:-translate-y-0.5 hover:bg-[var(--secondary)]"
					href="https://calendly.com/zaxkalyan/30min"
				>
					Book a call
				</a>
			</div>
		</section>

	</main>
</Layout>
